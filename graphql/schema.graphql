type Movie {
    id: Int!
    title: String!
    director: String!
    opening: String!
}

type Query {
    movies: [Movie]!
    movie(id: Int!): Movie
}

type Mutation {
    addMovie(title: String!, director: String!, opening: String!): Movie!
    deleteMovie(id: Int!): Boolean!
}